import'package:flutter/material.dart';

class FoldingCardboard extends StatelessWidget {
  const FoldingCardboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
